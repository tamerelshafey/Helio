
import React from 'react';

// This file appears to be a placeholder for a generic data context.
// In the current application structure, data fetching, caching, and state management
// are handled by `@tanstack/react-query` (React Query).
// Each component or feature fetches its own data using `useQuery`.
// This context is therefore unused and can likely be removed.

const DataContext: React.FC = () => {
  return null;
};

export default DataContext;
